# Security notes

- `--mock` is the default and never makes a network request.
- Live credentials are read from environment variables only.
- Do not commit `.env`, API keys, tokens, browser profiles, or proprietary art packs.
- The public showcase intentionally excludes original delivery HTML, audio/video, and extracted asset bundles.
